# 安装与依赖

这里收集第一次启动时最常见的问题：缺少依赖、编辑器按钮不出现，以及那种烦人的重启循环。

## 为什么第一次安装有时要第二次重启？

某些早期环境第一次可能会没加载上 `PySide6` 或 `Pillow` 这类依赖。如果编辑器启动按钮没出现，重新安装依赖，然后重启 Blender。

## 如果编辑器按钮没出现，该检查什么？

再跑一次依赖安装，然后再重启 Blender 一次。通常这就是最无聊的故障模式。

## 如果插件提示缺少包怎么办？

先相信面板，不要猜。先把缺失依赖修好，再重启 Blender 试一次。
