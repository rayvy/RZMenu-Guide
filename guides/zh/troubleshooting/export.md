# 导出怪相

这里放的是输出侧的奇怪问题：字体、垃圾文件，以及那些技术上成功了，但看起来还是不对劲的导出结果。

## 为什么没有可见文本时，RZMenu 还是会导出字体？

因为 UI 还是需要一个字体路径，用来处理内部消息、警告、依赖说明和未来的运行时文本。

正常情况下只会需要一个基础字体图集。如果额外字体图集无缘无故冒出来，那就是清理候选。

## 为什么会导出一些垃圾？

因为插件要么还需要它们，要么清理流程还没做完。

先试着删掉。如果控制台没动静而且导出还正常，那很好。如果坏了，就加回去。
